# M0 Micro CTF

The flag is encoded in `artifact.txt`.

Rules:

- Work only inside this fixture directory.
- Read the artifact before answering.
- Decode its hexadecimal payload.
- Return the exact `MILKSU{...}` flag and a one-sentence evidence note.
