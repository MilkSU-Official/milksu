{
  "manifest_version": 3,
  "name": "MilkSU Current Tab Bridge",
  "version": "0.1.0",
  "description": "Explicitly shares only the current tab title, URL, and visible text with a local MilkSU client.",
  "permissions": ["activeTab", "scripting", "storage"],
  "host_permissions": ["http://127.0.0.1/*"],
  "action": {
    "default_title": "Share current tab with MilkSU",
    "default_popup": "popup.html"
  }
}
