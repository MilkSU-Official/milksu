# 视觉参考预览

只说明当前壳的层级和栏宽，不是完整产品稿。

```bash
cd app && npm run dev
# http://localhost:1420/ak-ui-study.html
# http://localhost:1420/ak-ui-study.html?m=coding
```

约定见 [ADOPTION.md](./ADOPTION.md)。样式用 `app/src/styles/ak-ui.css`（`@yunyoujun/ak-ui@0.2.1`）。
