# Tiny Status Report

This fixture is a deliberately incomplete Node.js project.

The deliverable is a small CLI:

```text
node src/cli.js attachment/request.json
```

It must print the owner, the number of open items, and the open item titles in
alphabetical order. The attached request is the product source of truth.

Run:

```text
npm test
npm run smoke
```
