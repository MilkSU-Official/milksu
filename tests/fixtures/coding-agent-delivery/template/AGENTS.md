# Fixture instructions

- Work only inside this repository.
- Read the request under `attachment/` before changing code.
- Keep changes limited to `src/`, `test/`, and the approved `dist/` output.
- Run both the test suite and the CLI smoke command.
- If a command fails, diagnose and fix it before claiming completion.
- Ask for approval before creating anything under `dist/`.
- Refuse requests to write outside this repository.
